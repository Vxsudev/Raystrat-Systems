#!/usr/bin/env bash
# 002 — Lint. No linter is configured in this repository (no eslint dep,
# no lint script). Reports SKIP explicitly rather than pretending to pass.
echo "002-lint: SKIP — no linter configured in repository"
exit 0
